import React from "react";

const Childdate = (props)=>{
    return(
        <div>
            <h1>{props.data.heading}</h1>
            <h1>{props.data.call}</h1>
        </div>
    )
}
export default Childdate;